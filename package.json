{
  "name": "the-graph-fullstack-demo",
  "version": "1.0.0",
  "description": "完整的 The Graph 全栈 Demo - 包含 Solidity 智能合约、The Graph 子图和 React 前端",
  "author": "Katherine",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "verify": "hardhat verify"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.19.0",
    "typescript": "^5.3.0"
  }
}
