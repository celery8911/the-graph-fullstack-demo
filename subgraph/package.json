{
  "name": "simple-token-subgraph",
  "version": "1.0.0",
  "description": "The Graph 子图用于索引 SimpleToken 合约事件",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio simple-token-subgraph",
    "create-local": "graph create --node http://localhost:8020/ simple-token-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ simple-token-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 simple-token-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.56.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}
